local WindUI = loadstring(game:HttpGet("https://github.com/Footagesus/WindUI/releases/latest/download/main.lua"))()

local Window = WindUI:CreateWindow({
    Title = "Blased hub X key system",
    Icon = "door-open", -- lucide icon
    Author = "",
    Folder = "BOK84238",
    
    -- â†“ This all is Optional. You can remove it.
    Size = UDim2.fromOffset(580, 460),
    MinSize = Vector2.new(560, 350),
    MaxSize = Vector2.new(850, 560),
    Transparent = true,
    Theme = "Sky",
    Resizable = true,
    SideBarWidth = 200,
    BackgroundImageTransparency = 0,9,
    HideSearchBar = true,
    ScrollBarEnabled = false,
    
    -- â†“ Optional. You can remove it.
    User = {
        Enabled = true,
        Anonymous = true,
        Callback = function()
            print("clicked")
        end,
    },
    
    -- â†“ Key system configuration
    KeySystem = { 
        -- â†“ Optional. You can remove it.
        Key = { "DJ827KO493P", "5678" },
        
        Note = "Example Key System.",
        
        -- â†“ Optional. You can remove it.
        Thumbnail = {
            Image = "rbxassetid://140626083677696",
            Title = "",
        },
        
        -- â†“ Optional. You can remove it.
        URL = "https://go.linkify.ru/2Crb",
        
        -- â†“ Optional. You can remove it.
        SaveKey = false, -- automatically save and load the key.
    },
})

Window:EditOpenButton({
    Title = "Open Example UI",
    Icon = "zap",
    CornerRadius = UDim.new(0,20),
    StrokeThickness = 2,
    Color = ColorSequence.new( -- gradient
        Color3.fromHex("4b09db"), 
        Color3.fromHex("b809db")
    ),
    OnlyMobile = false,
    Enabled = true,
    Draggable = true,
})

local Tab = Window:Tab({
    Title = "main",
    Icon = "zap", -- optional
    Locked = false,
})

local Button = Tab:Button({
    Title = "Activate Script",
    Desc = "Activate only in-game",
    Locked = false,
    Callback = function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/mynamewendel-ctrl/Blessed-Hub-X-/refs/heads/main/Plants-Vs-Brainrots.lua"))() 
    end
})
